// Default layer name when no <g> elements are present
export const DEFAULT_LAYER_NAME = '0';

// Number of coordinates per point (x, y)
export const COORDS_PER_POINT = 2;

// Minimum coordinates needed for polyline/polygon (2 points = 4 coords)
export const MIN_POLYLINE_COORDS = 4;

// Arc/curve parsing constants
export const ARC_RADIUS_TOLERANCE = 0.0001;
export const ARC_POINT_TOLERANCE = 0.0001;
export const DEGREES_TO_RADIANS = 180;
export const ARC_CENTER_SIGN = -1;
export const ELLIPTICAL_ARC_SEGMENTS = 32;
