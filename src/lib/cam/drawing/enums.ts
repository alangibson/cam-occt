/**
 * Specifies the location where the drawing origin should be positioned
 */
export enum OriginLocation {
    /**
     * Position the origin at the bottom-left corner of the drawing bounds
     * This translates the drawing so that the minimum coordinates are at (0, 0)
     */
    BOTTOM_LEFT = 'BOTTOM_LEFT',
}
