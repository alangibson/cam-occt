/**
 * CAM / Plasma cutting constants
 */

/**
 * Default pierce height for plasma cutting (mm)
 */
export const DEFAULT_PIERCE_HEIGHT_MM = 1;

/**
 * Default pierce delay for plasma cutting (seconds)
 */
export const DEFAULT_PIERCE_DELAY = 0.5;

/**
 * Default cut height for plasma cutting (mm)
 */
export const DEFAULT_CUT_HEIGHT_MM = 1.5;

/**
 * Default feed rate for plasma cutting (mm/min)
 */
export const DEFAULT_FEED_RATE_MM = 1000;

/**
 * Default kerf width for plasma cutting (mm)
 */
export const DEFAULT_KERF_WIDTH_MM = 1.5;

/**
 * Default puddle jump height for plasma cutting (mm)
 */
export const DEFAULT_PUDDLE_JUMP_HEIGHT_MM = 0.5;

/**
 * Default plunge rate for plasma cutting (mm/min)
 */
export const DEFAULT_PLUNGE_RATE_MM = 500;

/**
 * Default rapid rate for plasma cutting (mm/min)
 */
export const DEFAULT_RAPID_RATE_MM = 3000;

/**
 * Imperial feed rate for plasma cutting (mm/min)
 */
export const IMPERIAL_FEED_RATE_MM = 2540;

/**
 * Percentage multiplier for calculations (100%)
 */
export const PERCENTAGE_MULTIPLIER = 100;

/**
 * Small tolerance for CAM calculations (mm)
 */
export const CAM_CALCULATION_TOLERANCE_MM = 0.001;

/**
 * Precision for G-code coordinate values (X, Y, Z, I, J, K)
 */
export const GCODE_COORDINATE_PRECISION = 4;

/**
 * Precision for G-code parameter values
 */
export const GCODE_PARAMETER_PRECISION = 3;
