/**
 * High-resolution tessellation for ellipse rendering (points)
 */
export const ELLIPSE_TESSELLATION_POINTS: number = 64;

/**
 * Minimum tessellation points for adaptive ellipse rendering
 */
export const MIN_TESSELLATION_POINTS = 8;

/**
 * Maximum tessellation points for adaptive ellipse rendering
 */
export const MAX_TESSELLATION_POINTS = 200;
