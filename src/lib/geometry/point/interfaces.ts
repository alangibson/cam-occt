export interface Point2D {
    x: number;
    y: number;
}

/**
 * Point geometry for use in shapes
 * A point geometry is just a Point2D with no additional properties
 */
export type PointGeometry = Point2D;
